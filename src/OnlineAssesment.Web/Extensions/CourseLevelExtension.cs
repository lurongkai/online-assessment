using OnlineAssesment.Domain;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace OnlineAssesment.Web.Extensions
{
    public static class CourseLevelExtension
    {
        //public static CourseLevel ToCourseLevel(this int courseLevel) {
        //    if (courseLevel == 1) { return CourseLevel.CoursewareDesignerLevel1; }
        //    if (courseLevel == 2) { return CourseLevel.CoursewareDesignerLevel2; }
        //    if (courseLevel == 3) { return CourseLevel.CoursewareDesignerLevel3; }

        //    throw new ArgumentException("courseLevel");
        //}

        //public static int FromCourseLevel(this CourseLevel courseLevel) {
        //    if (courseLevel == CourseLevel.CoursewareDesignerLevel1) { return 1; }
        //    if (courseLevel == CourseLevel.CoursewareDesignerLevel2) { return 2; }
        //    if (courseLevel == CourseLevel.CoursewareDesignerLevel3) { return 3; }

        //    throw new ArgumentException("courseLevel");
        //}
    }
}