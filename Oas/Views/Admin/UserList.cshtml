@*@model UserManageViewModel
@{
    ViewBag.Title = "UserList";
    Layout = "~/Views/Shared/_common-layout.cshtml";
}

<p>
    @Html.ActionLink("新建", "TeacherCreate", null, new {@class = "btn btn-primary"})
</p>
<p>
    <table class="table table-hover table-bordered">
        <tr>
            <th>用户名</th>
            <th>姓名</th>
            <th>角色</th>
            <th>操作</th>
        </tr>
        @foreach (var item in Model.Users) {
            <tr>
                <td>
                    @item.Username
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    @item.RoleName
                </td>
                <td>
                    @Html.ActionLink("编辑", "UserEdit", null, new {@class = "btn btn-primary btn-xs"})
                    @Html.ActionLink("删除", "UserDelete", null, new {@class = "btn btn-danger btn-xs"})
                </td>
            </tr>
        }
    </table>
</p>*@